server :
  port : 8081 #your desired port number

client:
  url: #your front-end url

backend:
  domain: #backend domain here

jwt:
  secret: #secret key for jwt

spring :
  mail :
    host: #smtp host
    port: #smtp port
    username: #smtp username
    password: #smtp password
    properties.mail.smtp :
      auth : true
      starttls :
        enable : true
  data :
    mongodb:
      host : #mongo host
      port : #mongo posrt
      database : social-media
      username : #mongo username
      password : #mongodb password
  servlet :
    multipart :
      max-file-size : 10MB
      max-request-size : 10MB
      enabled : true